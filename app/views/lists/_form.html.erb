<div class="container">
  <%= simple_form_for @list do |f| %>
    <%= f.input :name, placeholder: "Write the category films here" %>
    <%= f.submit 'Submit', class: 'btn btn-primary' %>
  <% end %>
</div>
