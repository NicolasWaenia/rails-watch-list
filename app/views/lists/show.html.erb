
<div class="container mt-1">
    <div class="card-show-title text-center">
      <h1 class="m-4">Movies 🎬</h1>
    </div>
  </div>

  <div class="row cards-movie">
    <% @list.bookmarks.each do |bookmark| %>
      <div class="col-md-3">
        <div class="card mb-4">
          <%= image_tag bookmark.movie.poster_url, alt: "movie poster", class: "card-img-top img-fluid img-thumbnail" %>
          <div class="card-body">
            <div class="card-rating d-flex justify-content-between">
              <p class="mb-0">Rating : <span class="h5"><%= bookmark.movie.rating.round(1) %></span> <i class="fas fa-star"></i></p>
            </div>

            <div class="card-title">
              <h5><%= bookmark.movie.title %></h5>
            </div>
            <div class="card-overview">
              <p><%= bookmark.movie.overview %></p>
            </div>
            <hr>
            <p><strong>Review</strong></p>
            <div class="card-review">
              <p><%= bookmark.comment %></p>
              <%= link_to "Delete", bookmark_path(bookmark), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "btn btn-primary text-end" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <div class="text-center">
    <%= link_to "Add a bookmark", new_list_bookmark_path(@list), class: "btn btn-primary" %>
    <%= link_to "Return to lists", lists_path, class: "btn btn-secondary" %>
  </div>
</div>
